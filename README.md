## Installation

To run the web API on your local computer, first install the **dependencies** by running the following command.

```bash
pip install -r requirements.txt
```

## Getting Started

To start the app in development mode run the following command in terminal

```bash
export FLASK_ENV=development && export FLASK_APP=main.py && flask run
```
